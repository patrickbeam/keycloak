version: "3"
services:
  keycloak1:
    image: ghcr.io/patrickbeam/keycloak-19.0.2:latest
    command: ["start", "--db postgres", "--db-url-host postgres", "--db-username admin", "--db-password admin", "--debug"]
    ports:
      - "80:8080"
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KC_PROXY=edge
      - KC_CACHE=ispn
      - KC_CACHE_STACK=ec2
      - JAVA_OPTS_APPEND=-'Djgroups.dns.query=headless-keycloak -Djgroups.s3.bucket_name=velo-keycloak-inifinispan -Djgroups.s3.region_name=us-east-2 -Djgroups.s3.access_key=AKIAQGBAL77ZKNQHGFJN -Djgroups.s3.secret_access_key=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'
      - kc_hostname_url=http://keycloak.cloudmast.io
      - KC_DB_URL_HOST=postgres
      - kc_db_username=admin
      - kc_db_password=admin
      - KC_DB_URL_PORT=5432
      - kc_url_db_database=postgres
    networks:
      - keycloak
  keycloak2:
    image: ghcr.io/patrickbeam/keycloak-19.0.2:latest
    command: ["start", "--db postgres", "--db-url-host postgres", "--db-username admin", "--db-password admin", "--debug"]
    ports:
      - "81:8080"
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KC_PROXY=edge
      - JAVA_OPTS_APPEND=-'Djgroups.dns.query=headless-keycloak -Djgroups.s3.bucket_name=velo-keycloak-inifinispan -Djgroups.s3.region_name=us-east-2 -Djgroups.s3.access_key=AKIAQGBAL77ZKNQHGFJN -Djgroups.s3.secret_access_key=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'
      - kc_hostname_url=http://keycloak.cloudmast.io
      - KC_CACHE=ispn
      - kc_hostname_url=http://keycloak2.cloudmast.io:81
      - KC_DB_URL_HOST=postgres
      - kc_db_username=admin
      - kc_db_password=admin
      - KC_DB_URL_PORT=5432
      - kc_url_db_database=postgres
    networks:
      - keycloak
  postgres:
    image: postgres:10.21
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=keycloak
    networks:
      - keycloak
networks:
  keycloak:
